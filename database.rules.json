{
    "rules": {
        ".read": true,
        ".write": true,
        "bookings": {
            ".indexOn": ["timestamp", "otpKey", "user_id"]
        },
        "activity": {
            ".indexOn": ["user_id"]
        }
    }
}
